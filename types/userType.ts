export type UserType = {
  name: string;
  role: string;
  clients?: string;
};

export type UserRhType = {
  store: string,
  name: string,
  email: string,
  ramal: string,
  setor: string,
  role: string,
  date: string;
  login_ad: string;
};